import 'package:flutter/material.dart';
import 'marketplace_tab.dart';

/// Alias export for TechnicianMarketplaceScreen pointing to MarketplaceTab.
class TechnicianMarketplaceScreen extends StatelessWidget {
  const TechnicianMarketplaceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const MarketplaceTab();
  }
}
